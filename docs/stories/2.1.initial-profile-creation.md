# Story 2.1: Initial Profile Creation

## Status
Done

## Story
**As a** newly registered user,
**I want** my profile to be created automatically,
**so that** I can start using the app immediately

## Acceptance Criteria
1. [x] Profile document created in `users` collection after registration
2. [x] Default values set (points: 0, level: 1, notificationRadius: 10km)
3. [x] User ID links profile to Appwrite account
4. [x] Profile accessible immediately after registration

## Tasks / Subtasks
- [x] **T1**: Create profile document on registration (AC: 1, 2)
- [x] **T2**: Set default gamification values (AC: 2)
- [x] **T3**: Link profile to user account (AC: 3)

## Dev Notes

### Implementation Files
| File | Description |
|------|-------------|
| `lib/features/profile/data/profile_repository.dart` | Profile creation |
| `lib/features/auth/data/auth_repository.dart` | Calls profile creation on signup |

### Key Implementation
```dart
Future<UserModel> createUserProfile({
  required String oderId,
  required String email,
  required String name,
}) async {
  final doc = await databases.createDocument(
    databaseId: databaseId,
    collectionId: 'users',
    documentId: userId,
    data: {
      'email': email,
      'name': name,
      'points': 0,
      'level': 1,
      'followersCount': 0,
      'followingCount': 0,
      'notificationRadius': 10.0,
      'createdAt': DateTime.now().toIso8601String(),
      'updatedAt': DateTime.now().toIso8601String(),
    },
  );
  return UserModel.fromDocument(doc);
}
```

### Testing
- [x] Profile created on registration
- [x] Default values correct
- [ ] Unit tests needed (QA pending)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01 | 1.0 | Initial implementation | Dev Team |
| 2026-01-23 | 1.1 | Converted to BMAD format | Architect |

## Dev Agent Record
### Files Changed
- `lib/features/profile/data/profile_repository.dart`
- `lib/features/profile/domain/user_model.dart`

### Completion Status
- [x] All acceptance criteria met
- [ ] Tests pending QA
- [x] Feature functional

## QA Results
### Review Status
PENDING - Needs unit tests (QA-002)
