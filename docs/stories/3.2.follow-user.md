# Story 3.2: Follow User

## Status
Done

## Story
**As a** user,
**I want** to follow other users,
**so that** I can see their events and activities in my feed

## Acceptance Criteria
1. [x] Follow button visible on user profiles
2. [x] Follow creates relation in database
3. [x] Cannot follow self
4. [x] Cannot follow same user twice
5. [x] Proper permissions set on follow relation

## Tasks / Subtasks
- [x] **T1**: Add follow button to profile UI (AC: 1)
- [x] **T2**: Implement follow() repository method (AC: 2, 3, 4)
- [x] **T3**: Set proper Appwrite permissions (AC: 5)
- [x] **T4**: Implement isFollowing() check (AC: 4)

## Dev Notes

### Implementation Files
| File | Description |
|------|-------------|
| `lib/features/social/data/social_repository.dart` | `follow()`, `isFollowing()` |
| `lib/features/profile/presentation/profile_screen.dart` | Follow button UI |

### Key Implementation
```dart
Future<void> follow({
  required String followerId,
  required String followingId,
}) async {
  if (followerId == followingId) {
    throw SocialException('Vous ne pouvez pas vous suivre vous-meme.');
  }

  // Check if already following
  final alreadyFollowing = await isFollowing(
    followerId: followerId,
    followingId: followingId,
  );

  if (alreadyFollowing) {
    throw SocialException('Vous suivez deja cet utilisateur.');
  }

  // Create follow relation
  await _databases.createDocument(
    databaseId: AppwriteConfig.databaseId,
    collectionId: _followsCollection,
    documentId: ID.unique(),
    data: {
      'followerId': followerId,
      'followingId': followingId,
      'createdAt': DateTime.now().toIso8601String(),
    },
    permissions: [
      Permission.read(Role.user(followerId)),
      Permission.read(Role.user(followingId)),
      Permission.delete(Role.user(followerId)),
    ],
  );
}
```

### Database Collection
- Collection: `followers`
- Fields: `followerId`, `followingId`, `createdAt`

### Testing
- [x] Follow creates relation
- [x] Self-follow prevented
- [x] Duplicate follow prevented
- [ ] Unit tests needed (QA pending)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01 | 1.0 | Initial implementation | Dev Team |
| 2026-01-23 | 1.1 | Converted to BMAD format | Architect |

## Dev Agent Record
### Files Changed
- `lib/features/social/data/social_repository.dart`

### Completion Status
- [x] All acceptance criteria met
- [ ] Tests pending QA
- [x] Feature functional

## QA Results
### Review Status
PENDING - Needs unit tests (QA-003)
