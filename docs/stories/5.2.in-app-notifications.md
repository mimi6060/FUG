# Story 5.2: In-App Notification Center

## Status
Done

## Story
**As a** user,
**I want** to see my notifications in the app,
**so that** I can stay informed about events and social activity

## Acceptance Criteria
1. [x] Notifications screen accessible from nav bar
2. [x] List of notifications with type icons
3. [x] Unread indicator (blue dot)
4. [x] Mark as read on tap
5. [x] Navigate to related content on tap
6. [x] Swipe to delete
7. [x] Mark all as read option
8. [x] Delete all option
9. [x] Pull-to-refresh

## Tasks / Subtasks
- [x] **T1**: Create notifications screen (AC: 1, 2)
- [x] **T2**: Implement notification repository (AC: 2)
- [x] **T3**: Add unread indicator (AC: 3)
- [x] **T4**: Implement mark as read (AC: 4)
- [x] **T5**: Add navigation handlers (AC: 5)
- [x] **T6**: Add swipe to dismiss (AC: 6)
- [x] **T7**: Add bulk actions menu (AC: 7, 8)
- [x] **T8**: Add refresh indicator (AC: 9)

## Dev Notes

### Implementation Files
| File | Description |
|------|-------------|
| `lib/features/notifications/presentation/notifications_screen.dart` | Screen UI |
| `lib/features/notifications/data/notification_repository.dart` | Data layer |
| `lib/features/notifications/domain/notification_model.dart` | Model |

### Key Implementation
```dart
class NotificationsScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final notificationsAsync = ref.watch(notificationsProvider);

    return Scaffold(
      appBar: AppBar(
        title: Text(l10n.notifications),
        actions: [
          PopupMenuButton<String>(
            onSelected: (value) async {
              switch (value) {
                case 'mark_all_read':
                  await repository.markAllAsRead(user.$id);
                  break;
                case 'delete_all':
                  // Confirm and delete
                  break;
              }
            },
            itemBuilder: (context) => [
              PopupMenuItem(value: 'mark_all_read', /* ... */),
              PopupMenuItem(value: 'delete_all', /* ... */),
            ],
          ),
        ],
      ),
      body: notificationsAsync.when(
        data: (notifications) => RefreshIndicator(
          onRefresh: () async {
            ref.invalidate(notificationsProvider);
          },
          child: ListView.builder(
            itemBuilder: (context, index) => Dismissible(
              key: Key(notifications[index].id),
              onDismissed: (_) => onDismiss(),
              child: _NotificationTile(/* ... */),
            ),
          ),
        ),
      ),
    );
  }
}
```

### Notification Types
- newEvent, eventReminder, eventCancelled, eventUpdated
- newParticipant, newFollower, eventLiked, newComment
- directMessage, system
- reportReceived, contentModerated, appealDecision (DSA)

### Icon Colors by Type
- Event: primary, orange, red, blue
- Social: green, pink, purple
- System: grey
- Reports: red

### Testing
- [x] Notifications load correctly
- [x] Mark as read works
- [x] Navigation to content works
- [x] Swipe delete works
- [ ] Unit tests needed (QA pending)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01 | 1.0 | Initial implementation | Dev Team |
| 2026-01-23 | 1.1 | Converted to BMAD format | Architect |

## Dev Agent Record
### Files Changed
- `lib/features/notifications/presentation/notifications_screen.dart`
- `lib/features/notifications/data/notification_repository.dart`
- `lib/features/notifications/domain/notification_model.dart`

### Completion Status
- [x] All acceptance criteria met
- [ ] Tests pending QA
- [x] Feature functional

## QA Results
### Review Status
PENDING - Needs unit tests (QA-005)
